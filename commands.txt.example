# One command per line. Blank lines and lines starting with # are ignored.
# Copy to commands.txt (the default for -f) or point at it with -f.
#
#   ssh-audits -i inventory/hosts -g mygroup

uname -a
uptime
df -h /
